<!-- last_compiled_event_ts: null -->
# Learned Behaviors

## Pinned Rules
Lines below starting with `[PINNED]` are hand-authored rules the dreaming compiler will never modify. Other lines below the `## Discovered` heading are written by the Phase 5 dreaming compiler from observed user reactions. In v1, the `## Discovered` section stays empty until Phase 5 ships.

[PINNED] Never use emojis in chat or speech bubbles.
[PINNED] Keep responses to 1–2 sentences unless explicitly asked for more.

## Discovered
(none yet)
